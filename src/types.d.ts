interface Cruise {
  entry_id: string;
  total_area: string;
  url: string;
  platform_id: string;
  year: string;
  is_rejected: 'f' | 't';
  created: string; // YYYY-MM-DD
  device_make: string;
  device_model: string;
}